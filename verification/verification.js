const db=window.supabase.createClient(ICA_CONFIG.supabaseUrl,ICA_CONFIG.supabaseKey),form=document.getElementById('verifyForm'),identityForm=document.getElementById('identityForm'),branchForm=document.getElementById('branchForm'),box=document.getElementById('result'),input=document.getElementById('credentialNo');
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function field(label,value){return value?`<div class="field"><small>${label}</small><b>${esc(value)}</b></div>`:''}
function kind(d){const text=`${d.detail_name||''} ${d.type_name||''} ${d.position_title||''}`.toLowerCase();if(/branch|지부/.test(text))return'BRANCH';if(d.position_title||/appointment|임명/.test(text))return'APPOINTMENT';if(/institution|organization|기관|인증기관/.test(text))return'ORGANIZATION';return'CREDENTIAL'}
function statusLabel(d,k){const s=String(d.status||'VALID').toUpperCase();if(s!=='VALID')return s;if(k==='APPOINTMENT')return'ACTIVE';if(k==='BRANCH')return'REGISTERED';if(k==='ORGANIZATION')return'ACCREDITED';return'VALID'}
function titleFor(k){return({CREDENTIAL:'OFFICIAL CREDENTIAL RECORD',APPOINTMENT:'OFFICIAL APPOINTMENT RECORD',BRANCH:'OFFICIAL BRANCH RECORD',ORGANIZATION:'OFFICIAL ORGANIZATION RECORD'})[k]}
function imageFor(d){const src=d.photo_url||'../assets/%20images/ica-official-emblem..png';return`<div class="portrait ${d.photo_url?'person':'fallback'}"><img src="${esc(src)}" alt="Official record image" onerror="this.src='../assets/%20images/ica-official-emblem..png';this.parentElement.classList.add('fallback')"></div>`}
function shareUrl(no){const u=new URL(location.href);u.search='';u.searchParams.set('code',no);return u.toString()}
async function copyLink(no){try{await navigator.clipboard.writeText(shareUrl(no));alert('ICA 공식 조회 링크를 복사했습니다.')}catch{prompt('조회 링크를 복사하세요.',shareUrl(no))}}
async function shareRecord(no){const url=shareUrl(no);if(navigator.share){try{await navigator.share({title:'ICA VERIFIED · '+no,text:'ICA Official Verification Record',url})}catch{}}else copyLink(no)}
function card(d,compact=false){const k=kind(d),s=statusLabel(d,k),name=d.person_name_en||d.person_name||d.qualification_name||d.issuer_name||'ICA OFFICIAL RECORD',subtitle=d.position_title||d.qualification_name||d.rank_grade||d.detail_name||d.type_name||'';if(compact)return`<button class="found-card" onclick="verify('${esc(d.credential_no)}')">${imageFor(d)}<span><small>${esc(titleFor(k))}</small><b>${esc(d.person_name||name)}</b><em>${esc(subtitle||d.type_name||'Credential')}</em><strong>${esc(d.credential_no)}</strong></span><i>${esc(s)} →</i></button>`;return`<article class="credential-card"><div class="verified-head"><div><span class="verified-check">✓</span><b>ICA VERIFIED</b><small>${titleFor(k)}</small></div><strong class="record-status ${esc(d.status)}">${esc(s)}</strong></div><div class="identity">${imageFor(d)}<div class="identity-copy"><p class="record-type">${esc(k)}</p><h3>${esc(name)}</h3>${d.person_name&&d.person_name!==name?`<p class="local-name">${esc(d.person_name)}</p>`:''}${subtitle?`<h4>${esc(subtitle)}</h4>`:''}<p class="record-no">${esc(d.credential_no)}</p></div></div><div class="fields">${field('Issuing Organization · 발급기관',d.issuer_name)}${field('Credential Type · 종류',d.detail_name||d.type_name)}${field('Name · 성명',d.person_name)}${field('English Name · 영문명',d.person_name_en)}${field('Qualification / Position · 자격·직책',d.qualification_name||d.position_title)}${field('Discipline · 종목',d.discipline)}${field('Rank / Grade · 단·등급',d.rank_grade)}${field('Issued · 발급일',d.issued_on)}${field('Valid Until · 유효기간',d.valid_until)}</div><div class="official-seal"><img src="../assets/%20images/ica-official-emblem..png" alt="ICA"><div><small>REGISTERED & VERIFIED BY</small><b>ICA · INTERNATIONAL CREDENTIAL ALLIANCE</b><span>This screen is an official public verification record.</span></div></div><div class="record-actions"><button onclick="copyLink('${esc(d.credential_no)}')">COPY LINK</button><button onclick="shareRecord('${esc(d.credential_no)}')">SHARE</button><button onclick="window.print()">PRINT</button></div></article>`}
function render(d){box.innerHTML=card(d)}
async function verify(no){no=String(no||'').trim().toUpperCase();if(!no)return;box.classList.remove('hidden');box.innerHTML='<p class="checking">ICA Global Registry를 확인하고 있습니다…</p>';const{data,error}=await db.rpc('ica_verify_credential',{p_credential_no:no});if(error||!data){box.innerHTML=`<div class="not-found"><strong>NOT FOUND</strong><b>ICA GLOBAL REGISTRY</b><p>발급번호를 다시 확인하거나 다른 조회방식을 이용해 주세요.</p></div>`;return}render(data)}
async function findIdentity(name,birth){name=String(name||'').trim();if(!name||!birth)return;box.classList.remove('hidden');box.innerHTML='<p class="checking">이름과 생년월일이 일치하는 ICA 기록을 찾고 있습니다…</p>';const{data,error}=await db.rpc('ica_find_credentials_by_identity',{p_name:name,p_birth_date:birth});if(error){box.innerHTML='<div class="not-found"><strong>SEARCH ERROR</strong><p>잠시 후 다시 시도해 주세요.</p></div>';return}const rows=Array.isArray(data)?data:[];if(!rows.length){box.innerHTML='<div class="not-found"><strong>일치하는 기록이 없습니다.</strong></div>';return}box.innerHTML=`<div class="found-head"><b>${esc(name)}님의 Credential ${rows.length}건</b><small>확인할 기록을 선택하세요.</small></div><div class="found-list">${rows.map(x=>card(x,true)).join('')}</div>`}
function branchShareUrl(code){const u=new URL(location.href);u.search='';u.searchParams.set('branch',code);return u.toString()}
async function copyBranchLink(code){try{await navigator.clipboard.writeText(branchShareUrl(code));alert('ICA 공식 지부 조회 링크를 복사했습니다.')}catch{prompt('조회 링크를 복사하세요.',branchShareUrl(code))}}
function branchProfile(d){const photo=d.leader_photo_url||'../assets/%20images/ica-official-emblem..png';return`<article class="credential-card branch-profile"><div class="verified-head"><div><span class="verified-check">✓</span><b>ICA VERIFIED</b><small>OFFICIAL INTERNATIONAL BRANCH</small></div><strong class="record-status">ACTIVE</strong></div><div class="identity"><div class="portrait ${d.leader_photo_url?'person':'fallback'}"><img src="${esc(photo)}" alt="${esc(d.leader_name)}" onerror="this.src='../assets/%20images/ica-official-emblem..png';this.parentElement.classList.add('fallback')"></div><div class="identity-copy"><p class="record-type">INTERNATIONAL BRANCH OFFICIAL PROFILE</p><h3>${esc(d.leader_name)}</h3><h4>${esc(d.position_title||'President')}</h4><p class="branch-location">🇺🇸 ${esc(d.city_or_region)}, ${esc(d.country)}</p><p class="record-no">${esc(d.branch_code)}</p></div></div><div class="fields">${field('Organization · 소속','International Police Martial Arts Alliance (IPMA)')}${field('Official Position · 공식직책',d.position_title||'President')}${field('Branch · 지부',d.branch_name)}${field('Region · 지역',d.city_or_region)}${field('Country · 국가',d.country)}${field('Branch Status · 지부상태','ACTIVE · OFFICIALLY VERIFIED')}${field('Official Branch No. · 공식지부번호',d.branch_code)}${field('Member / Leader No.',d.external_member_id)}${field('Appointment Date · 임명일',d.appointment_date)}</div><div class="official-seal"><img src="../assets/%20images/ica-official-emblem..png" alt="ICA"><div><small>OFFICIAL GLOBAL BRANCH REGISTRY</small><b>ICA · INTERNATIONAL CREDENTIAL ALLIANCE</b><span>Registered and verified international branch record.</span></div></div><div class="record-actions"><button onclick="copyBranchLink('${esc(d.branch_code)}')">COPY LINK</button><button onclick="window.print()">PRINT</button></div></article>`}
async function findBranch(q){q=String(q||'').trim();if(!q)return;box.classList.remove('hidden');box.innerHTML='<p class="checking">ICA International Branch Registry를 확인하고 있습니다…</p>';const{data,error}=await db.rpc('ica_find_public_branches',{p_query:q});if(error){box.innerHTML='<div class="not-found"><strong>SEARCH ERROR</strong><p>잠시 후 다시 시도해 주세요.</p></div>';return}const rows=Array.isArray(data)?data:[];if(!rows.length){box.innerHTML='<div class="not-found"><strong>BRANCH NOT FOUND</strong><p>이름, 국가, 주 또는 도시를 다시 확인해 주세요.</p></div>';return}if(rows.length===1){box.innerHTML=branchProfile(rows[0]);return}box.innerHTML=`<div class="found-head"><b>International Branch ${rows.length}건</b><small>확인할 지부를 선택하세요.</small></div><div class="found-list">${rows.map((d,i)=>`<button class="found-card branch-found" onclick="showBranch(${i})"><span><small>OFFICIAL BRANCH</small><b>${esc(d.branch_name)}</b><em>${esc(d.leader_name)} · ${esc(d.position_title)}</em><strong>${esc(d.city_or_region)}, ${esc(d.country)}</strong></span><i>ACTIVE →</i></button>`).join('')}</div>`;window._branchRows=rows}
function showBranch(i){box.innerHTML=branchProfile(window._branchRows[i]);box.scrollIntoView({behavior:'smooth'})}
function mode(which){const identity=which==='identity',branch=which==='branch';document.getElementById('tabNo').classList.toggle('active',!identity&&!branch);document.getElementById('tabIdentity').classList.toggle('active',identity);document.getElementById('tabBranch').classList.toggle('active',branch);form.classList.toggle('hidden',identity||branch);identityForm.classList.toggle('hidden',!identity);branchForm.classList.toggle('hidden',!branch);document.getElementById('searchHelp').textContent=branch?'이름·국가·주·도시·공식 지부번호 중 하나만 입력해도 검색됩니다.':identity?'성명과 생년월일이 정확히 일치할 때만 등록 기록을 보여줍니다.':'QR로 접속하면 발급번호가 자동 입력됩니다.';box.classList.add('hidden')}
document.getElementById('tabNo').onclick=()=>mode('no');document.getElementById('tabIdentity').onclick=()=>mode('identity');document.getElementById('tabBranch').onclick=()=>mode('branch');form.addEventListener('submit',e=>{e.preventDefault();verify(input.value)});identityForm.addEventListener('submit',e=>{e.preventDefault();findIdentity(document.getElementById('searchName').value,document.getElementById('searchBirth').value)});branchForm.addEventListener('submit',e=>{e.preventDefault();findBranch(document.getElementById('branchQuery').value)});const qs=new URLSearchParams(location.search),q=qs.get('code')||qs.get('no'),bq=qs.get('branch');if(q){input.value=q;verify(q)}else if(bq){mode('branch');document.getElementById('branchQuery').value=bq;findBranch(bq)}
window.copyLink=copyLink;window.shareRecord=shareRecord;window.verify=verify;window.copyBranchLink=copyBranchLink;window.showBranch=showBranch;
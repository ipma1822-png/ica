(()=>{const hero=document.querySelector('.global-hero'),stage=document.querySelector('.emblem-stage'),img=stage&&stage.querySelector('img'),o1=document.querySelector('.orbit1'),o2=document.querySelector('.orbit2');if(!hero||!stage||!img)return;const field=document.createElement('canvas');field.className='ica-live-canvas';field.setAttribute('aria-hidden','true');Object.assign(field.style,{position:'absolute',inset:'0',width:'100%',height:'100%',zIndex:'1',pointerEvents:'none'});hero.prepend(field);const ctx=field.getContext('2d');let w=0,h=0,dpr=1;const nodes=Array.from({length:34},(_,i)=>({x:(i*73%997)/997,y:(i*137%991)/991,vx:((i%5)-2)*.012,vy:(((i*3)%5)-2)*.008,r:1.3+(i%3)*.55}));function resize(){const r=hero.getBoundingClientRect();dpr=Math.min(devicePixelRatio||1,2);w=r.width;h=r.height;field.width=Math.max(1,Math.round(w*dpr));field.height=Math.max(1,Math.round(h*dpr));ctx.setTransform(dpr,0,0,dpr,0,0)}resize();addEventListener('resize',resize,{passive:true});let start=performance.now();function frame(now){const t=(now-start)/1000;ctx.clearRect(0,0,w,h);for(const n of nodes){n.x=(n.x+n.vx*.016+1)%1;n.y=(n.y+n.vy*.016+1)%1}for(let i=0;i<nodes.length;i++){const a=nodes[i],ax=a.x*w,ay=a.y*h;for(let j=i+1;j<nodes.length;j++){const b=nodes[j],bx=b.x*w,by=b.y*h,dx=ax-bx,dy=ay-by,d=Math.hypot(dx,dy);if(d<155){ctx.strokeStyle=`rgba(85,155,240,${.25*(1-d/155)})`;ctx.lineWidth=.8;ctx.beginPath();ctx.moveTo(ax,ay);ctx.lineTo(bx,by);ctx.stroke()}}const glow=.45+.55*Math.sin(t*2.1+i);ctx.fillStyle=`rgba(244,194,72,${.35+.55*Math.max(0,glow)})`;ctx.shadowColor='rgba(244,194,72,.85)';ctx.shadowBlur=10;ctx.beginPath();ctx.arc(ax,ay,a.r*(1+.35*Math.sin(t*2+i)),0,Math.PI*2);ctx.fill()}ctx.shadowBlur=0;const bob=Math.sin(t*1.45)*15,scale=1+Math.sin(t*1.8)*.018;stage.style.transform=`translateY(${bob}px)`;img.style.transform=`scale(${scale})`;img.style.filter=`drop-shadow(0 25px 40px #000) drop-shadow(0 0 ${22+12*(1+Math.sin(t*1.8))}px rgba(228,184,79,.38))`;if(o1)o1.style.transform=`rotate(${t*22}deg)`;if(o2)o2.style.transform=`rotate(${-t*15}deg)`;requestAnimationFrame(frame)}requestAnimationFrame(frame);const reveal=document.querySelectorAll('.service-card,.trust-grid>*,.section-kicker');reveal.forEach(el=>el.classList.add('ica-reveal'));if('IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.1});reveal.forEach(el=>io.observe(el))}else reveal.forEach(el=>el.classList.add('is-visible'));})();